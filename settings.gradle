rootProject.name = 'propertee-teebox'

includeBuild('../propertee-java') {
    dependencySubstitution {
        substitute module('com.propertee:propertee-core') using project(':propertee-core')
    }
}

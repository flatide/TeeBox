rootProject.name = 'propertee-teebox'

includeBuild('../propertee2-java') {
    dependencySubstitution {
        substitute module('com.flatide:propertee-core') using project(':propertee-core')
    }
}

// Long-running external task: use admin page or API to kill the task while SHELL is blocking
// Submit with property: scriptsDir = /path/to/demo/teebox/scripts
PRINT("starting long task via SHELL")
ctx = SHELL_CTX(scriptsDir)
result = SHELL(ctx, "./long_sleep.sh")
PRINT("final result", result)
